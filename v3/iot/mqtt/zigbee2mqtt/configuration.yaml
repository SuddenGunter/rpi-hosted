version: 4

homeassistant:
    enabled: false

frontend:
    enabled: true
    port: 8080

mqtt:
    base_topic: zigbee2mqtt
    server: 'mqtt://mqtt'
    user: my_user
    password: my_password

serial:
    port: /dev/ttyACM0
    adapter: deconz

availability:
    enabled: true

advanced:
    network_key: GENERATE
    pan_id: GENERATE
    ext_pan_id: GENERATE
